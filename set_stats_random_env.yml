---
- name: Randomly set environment
  hosts: all
  gather_facts: false
  connection: local

  tasks:
    - name: Randomly choose an environment
      ansible.builtin.set_stats:
        data:
          environment: "{{ ['prod', 'dev'] | random }}"
